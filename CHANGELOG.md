# 变更日志（CHANGELOG）

本文件记录每个版本的可见变化。版本号遵循语义化版本（主.次.修）。

## [Unreleased]

## [1.0.1] - 2026-08-08

### 修复（来自第二轮外部只读代码审查）
- **[M1]** 归档完成时若"移动成功但撤销记录写入失败"，GUI 此前会忽略告警、只显示绿色成功提示并禁用撤销按钮。现已在弹窗与状态栏追加该警告，用户可知原因。
- **[M2]** `gen_bat.py` 原以 GBK 写盘、但 .bat 内含 `chcp 65001`，导致中文标题/提示在 cmd 下乱码。改为以 UTF-8 BOM（`utf-8-sig`）写盘，与 `chcp 65001` 一致，中文正常显示。
- **[M3]** `_looks_like_our_dir` 对英文类别名（如 `Photos`）大小写敏感，导致二次运行未被识别为已分类、可能被重复扫描。已加 `re.IGNORECASE`。
- **[I1]** `_SCREEN_SIZES` 分辨率集合存在重复项 `(1080, 1920)`，已清理。
- **[I2]** `_on_scan_row` 追加识别结果时未同步 `_item_index`，已补同步（无实际风险，纯健壮性）。

## [1.0.0] - 2026-08-08

### 新增
- 初版发布：照片 / 视频智能分类整理桌面程序。
- 多类别语义识别（本地 CLIP ViT-B/32，CPU，离线）：人像 / 风景 / 博物馆 / 飞机 / 宠物 / 事物 / 证件 / 动图 / 漫画 / 插画 / 美食 / 建筑 / 车辆 等，类别清单可在 `categories.json` 编辑。
- 自动编号 `类别 + 日期(可选) + 两位序号`，如 `人像01.jpg`、`人像21-01.jpg`、`2021-人像/人像01.jpg`。
- 支持 JPG/PNG/GIF/WEBP/RAW 与 MP4/MOV/FLV 等；RAW 读相机预览图，视频抽 3 帧。
- 同目录内建子文件夹；可选连子文件夹一起整理。
- 安全归档：同盘 rename / 跨盘 copy2 + 大小校验；撤销日志可一键还原；防覆盖双保险。
- 跨平台：Windows 双击 `启动.bat`；macOS 运行 `启动.command`。
- 四类硬约束（绝不重编码 / 降画质 / 覆盖 / 篡改）写死在代码并纳入自检。
- 自检脚本 `selftest.py`：造样本 → 归档 → 逐字节 MD5 校验 → 撤销还原。
- 开源协作配套：MIT 许可证、README、CONTRIBUTING、.gitignore、VERSION。

### 安全
- 全程离线、零上传、零遥测；运行时无任何云端 API 调用。
- 经外部只读代码审查（P1–P8）并修复：竞态覆盖、撤销跨盘、撤销记录写入健壮性、用户文件夹误跳、截图规则误判、安装脚本 CPU torch / 去冗余依赖、O(n²) 索引、死代码清理、像素上限保护。
