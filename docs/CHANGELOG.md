# 变更日志（CHANGELOG）

本文件记录每个版本的可见变化。版本号遵循语义化版本（主.次.修）。

## [Unreleased]

## [1.0.2] - 2026-08-08

### 新增
- **时间来源开关**：界面新增「日期来源」下拉，支持「拍摄时间(EXIF)」与「文件创建时间」两种模式；`core/media.py` 的 `probe()` 按模式选择时间源。
- **时间板块 + 日期文件名**：勾选「时间板块」后，文件夹按年/月分（如 `2026人像`、`2026-08人像`），文件名仍保留完整日期（如 `人像2026-08-25-01.jpg`），解决同文件夹内不同月份照片难定位的问题。
- **拍摄时间增强识别**：引入 `exifread` 读取 PIL 读不到的 SubIFD/MakerNote；增加文件名日期兜底（支持 `IMG_20260825_123456`、`2026-08-25`、`2026.08.25` 等）；视频优先读取 ffmpeg `creation_time` 并按 UTC/时区转本地时间。
- **归档前时间来源报告**：执行归档前弹窗展示每张照片的时间来源分布（EXIF / 文件名 / 视频元数据 / 文件时间），并重点列出非 EXIF 的照片，让用户先核对日期真实性。
- **撤销栈扩展到 5 次**：每次归档保留最近 5 条 undo 记录；UI 提供「撤销上一步」与「撤销到…」菜单，可一次性还原到任意历史步骤之前。
- **示例截图**：新增 `docs/screenshots/v1.0.2/` 示例图，展示新命名结构、时间板块与界面设置。
- **许可证改为 CC BY-NC 4.0**：自 v1.0.2 起，本项目采用「知识共享 署名-非商业性使用 4.0 国际（CC BY-NC 4.0）」许可，**禁止任何商业用途**，仅允许非商业性的分享与演绎；商业场景须获作者书面授权。

### 修复
- 修复 `categories.json` 中部分类别 prompts 为中文导致 OpenCLIP tokenizer 误分的问题（如人像进星空），全部类别改为具体英文描述。
- 修复 `app.py` 缺少 `datetime`/`json` 导入导致「执行归档」按钮点击无反应的问题。
- 修复时间板块开启时文件名被强制省略日期的问题。
- 修复跨月份照片在同一文件夹内序号串号的问题（序号按「文件夹+日期段」分组递增）。
- 修复 ffmpeg `creation_time` 因微秒段干扰导致时区未正确捕获、视频时间偏差 8 小时的问题。
- 修复视频文件未正确标记 `time_source` 的问题。

## [1.0.1] - 2026-08-08

### 修复（来自第二轮外部只读代码审查）
- **[M1]** 归档完成时若"移动成功但撤销记录写入失败"，GUI 此前会忽略告警、只显示绿色成功提示并禁用撤销按钮。现已在弹窗与状态栏追加该警告，用户可知原因。
- **[M2]** `gen_bat.py` 原以 GBK 写盘、但 .bat 内含 `chcp 65001`，导致中文标题/提示在 cmd 下乱码。改为以 UTF-8 BOM（`utf-8-sig`）写盘，与 `chcp 65001` 一致，中文正常显示。
- **[M3]** `_looks_like_our_dir` 对英文类别名（如 `Photos`）大小写敏感，导致二次运行未被识别为已分类、可能被重复扫描。已加 `re.IGNORECASE`。
- **[I1]** `_SCREEN_SIZES` 分辨率集合存在重复项 `(1080, 1920)`，已清理。
- **[I2]** `_on_scan_row` 追加识别结果时未同步 `_item_index`，已补同步（无实际风险，纯健壮性）。

### 完全离线分发
- 将 CLIP 模型权重（`open_clip_model.safetensors`，约 600MB）随 exe 一并打包，任何电脑断网也能直接识别，无需首次联网下载。
- `core/classifier.py` 新增 `_bundle_weights_path()`：打包后优先从 `sys._MEIPASS/models/` 读取内置权重，绝不联网；开发态仍走「首次联网下载」逻辑，向后兼容。
- `build_exe.py` 产出 `照片视频智能分类整理_windows_v1.0.1_offline.zip`（含权重，约 850MB）；另保留标准版 `..._windows_v1.0.1.zip`（不含权重，约 240MB，适合已下载过权重的电脑）。

### 第三轮外部审查（Info 级，无阻塞）
- **[I3]** `_set_folder` 切换文件夹时未重置 `_item_index` → 已在 `_set_folder` 加 `self._item_index = {}` 保持状态一致（无实际风险，纯健壮）。
- **[I4]** `.bat` 采用 UTF-8 BOM + `chcp 65001`，在极旧 Windows（Win8/早期 Win10）上首行可能报噪音；审查者确认对主流 Win10/11 合理、无需强制修改，且退回 GBK 会重新乱码，故保持现状。
- `build_exe.py`：沙箱禁止 `shutil.rmtree`/`rmdir` 删除，旧产物改由 PyInstaller `--noconfirm` 自行覆盖，构建步骤不再手动删除。

## [1.0.0] - 2026-08-08

### 新增
- 初版发布：照片 / 视频智能分类整理桌面程序。
- 多类别语义识别（本地 CLIP ViT-B/32，CPU，离线）：人像 / 风景 / 博物馆 / 飞机 / 宠物 / 事物 / 证件 / 动图 / 漫画 / 插画 / 美食 / 建筑 / 车辆 等，类别清单可在 `categories.json` 编辑。
- 自动编号 `类别 + 日期(可选) + 两位序号`，如 `人像01.jpg`、`人像21-01.jpg`、`2021-人像/人像01.jpg`。
- 支持 JPG/PNG/GIF/WEBP/RAW 与 MP4/MOV/FLV 等；RAW 读相机预览图，视频抽 3 帧。
- 同目录内建子文件夹；可选连子文件夹一起整理。
- 安全归档：同盘 rename / 跨盘 copy2 + 大小校验；撤销日志可一键还原；防覆盖双保险。
- 跨平台：Windows 双击 `启动.bat`；macOS 运行 `启动.command`。
- 四类硬约束（绝不重编码 / 降画质 / 覆盖 / 篡改）写死在代码并纳入自检。
- 自检脚本 `selftest.py`：造样本 → 归档 → 逐字节 MD5 校验 → 撤销还原。
- 开源协作配套：MIT 许可证、README、CONTRIBUTING、.gitignore、VERSION。

### 安全
- 全程离线、零上传、零遥测；运行时无任何云端 API 调用。
- 经外部只读代码审查（P1–P8）并修复：竞态覆盖、撤销跨盘、撤销记录写入健壮性、用户文件夹误跳、截图规则误判、安装脚本 CPU torch / 去冗余依赖、O(n²) 索引、死代码清理、像素上限保护。
