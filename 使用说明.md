# 照片 / 视频 智能分类整理

把一个乱糟糟的照片文件夹拖进来，它会看懂每一张图的内容，在**这个文件夹内部**建好类别子文件夹，把文件归位，并按 `类别+日期-序号` 重新编号。

---

## 一、启动

双击 **`启动.bat`**。

第一次用，如果提示"找不到运行环境"，先双击 **`安装依赖.bat`**。

---

## 二、三步走

| 步骤 | 做什么 |
|---|---|
| **拖** | 把文件夹拖到窗口顶部的白色区域（也可以点一下选择） |
| **①识别** | 点「开始识别（不动文件）」。**这一步绝对不碰你的文件**，只是把识别结果列出来给你看 |
| **②归档** | 核对无误后点「执行归档」，确认弹窗，完成 |

结果不满意？点右下角 **「撤销上次整理」**，全部原样搬回去。

> 表格里**双击任意一行**可以手动改它的类别，改完编号会自动重排。

---

## 三、编号规则

格式：`类别 + 日期段(可选) + - + 两位序号`

```
人像01.jpg            风景01.jpg            动图01.gif        ← 纯序号（日期段留空）
人像21-01.jpg         风景21-01.jpg                             ← 手动日期段（填 21）
人像2021-08-01-01.jpg 风景2023-05-09-01.jpg                    ← 期日编号（自动取创建时间）
```

### 日期段怎么来的？三种选择

| 你想要 | 怎么设 |
|---|---|
| **纯 1、2、3 序号**（不确定日期 / 不想带日期） | 「期日编号」**不勾** + 「日期段」框**留空** → `人像01.jpg` |
| **自动带照片创建时间**（推荐） | 勾上「期日编号」，粒度选 年 / 年-月 / 年-月-日 → `人像2021-08-01-01.jpg` |
| **固定手动日期** | 「期日编号」不勾，在「日期段」框填 `21`/`2021`/`2108` 等 → `人像21-01.jpg` |

- 期日编号的日期**自动读取照片 EXIF 拍摄时间**，没有 EXIF 就退而用文件时间；两种都读不到则自动退化成纯序号，不会报错。
- 粒度说明：选「年」→ `人像2023-01`；「年-月」→ `人像2023-07-01`；「年-月-日」→ `人像2023-07-09-01`。

### 时间板块（可选，文件夹名 = 时间-类别）

勾上「时间板块」后，文件夹名直接就是「时间-类别」，时间标签来自你填的「日期段」或「期日编号」自动取的创建时间：

```
未勾时间板块：          勾了时间板块（按年）：
人像/                   2021-人像/
├─ 人像01.jpg          ├─ 人像01.jpg
└─ 人像02.jpg          └─ 人像02.jpg
风景/                   2022-人像/   ← 不同年份各从 01 开始
└─ 风景01.jpg          ├─ 人像01.jpg
                        └─ 人像02.jpg
                      2023-人像/
                      ├─ 人像01.jpg
                      └─ 人像02.jpg
```

- 文件夹名格式：`2021-人像` / `2021-08-风景`（时间标签来自日期段或期日编号）。
- 勾了时间板块时，时间已经体现在文件夹名里，**文件名不再重复带日期**，文件名就是 `类别+序号`（如 `人像01.jpg`）。
- 不勾时间板块时，就是 `人像/人像01.jpg` 这种平铺结构。
- 如果勾了时间板块却又没填日期段、也没勾期日编号，会退化成普通 `类别/` 文件夹。
- 读不到时间的文件（无 EXIF 也无文件时间）：勾了时间板块也取不到时间，会落进普通的 `类别/` 文件夹。

### 共通规则

- 每个文件夹**独立**从 01 开始编号；同一文件夹内序号连续。例如 `2021-人像` 从 01 开始，`2022-人像` 也从 01 开始，互不影响。
- 超过 99 张自动变成三位 `100`，不会撞号。
- 同一个文件夹**重复整理**时，序号会接着上次往后排，不会覆盖已有的。
- 勾选「新名保留原文件名」→ `人像2023-07-09-01_IMG_2233.jpg`。
- 同类内部按**拍摄时间**排序编号（读 EXIF，没有就用文件时间）。

---

## 四、类别清单可以随便改

点界面右侧 **「编辑类别清单」**：

- **名称** = 文件夹名，也是编号前缀
- **语义描述** = 英文，一行一条。这是识别的关键，写得越具体越准
- **特殊规则**：
  - `animated` → 动图（GIF、动态 WEBP），按文件特征判定，100% 准确
  - `screenshot` → 截图，按文件名和分辨率判定
  - `video` → 视频单独归类（需在主界面勾选「视频单独归类」）
  - 留空 → 纯靠语义识别

也可以直接用记事本编辑 `categories.json`。

**内置类别**：动图、截图、证件、人像、风景、博物馆、飞机、宠物、漫画、插画、美食、建筑、车辆、事物，识别不出来的进「其它」。

**想加新类别**（比如"婚礼"）：点新增 → 名称填「婚礼」→ 语义描述写：
```
a photo of a wedding ceremony
a photo of a bride in a white wedding dress
a photo of a wedding banquet hall
```

**把握阈值**：滑块调低 → 更激进，什么都往类别里塞；调高 → 更保守，拿不准的都进「其它」。默认 0.22 比较合适。

---

## 五、四条硬约束（写死在代码里）

| 约束 | 怎么做到的 |
|---|---|
| **绝不重编码** | 只调用文件系统的移动/改名，同盘操作是原子级的，一个字节都不重写 |
| **绝不降画质** | 识别时的缩放全部发生在内存副本上，原文件只读打开 |
| **绝不覆盖** | 目标位置一旦有同名文件，立刻换号；底层用 `os.rename`，目标存在会直接报错拒绝 |
| **绝不篡改** | 不写 EXIF、不改扩展名、不改拍摄时间，跨盘复制时逐字节校验大小 |

自检脚本 `selftest.py` 会造一批样本跑完整流程，逐个文件比对 MD5，验证上面四条。跑一下：

```
python selftest.py
```

---

## 六、支持的格式

- **图片**：JPG / JPEG / PNG / GIF / WEBP / BMP / TIF / TIFF
- **RAW**：CR2 / CR3 / NEF / ARW / DNG / ORF / RW2 / RAF / PEF / SRW 等（读相机内嵌预览图，秒开）
- **视频**：MP4 / MOV / FLV / M4V / AVI / MKV / WMV / WEBM / 3GP（抽 3 帧综合判断内容）
- HEIC/HEIF 需额外装 `pillow-heif`，否则会归到「其它」

---

## 七、成本

**0 元，0 积分，永久。**

识别用的是装在你电脑里的本地视觉模型（CLIP ViT-B/32），跑在 CPU 上，**全程离线、不联网、不上传任何照片**。唯一一次联网是第一次下载模型（约 600MB），之后拔网线也能用。

你的 i7-10750H 大约每张 0.1 秒，1000 张照片两分钟左右。核显不参与，不影响。

---

## 八、常见问题

**Q：第一次点「开始识别」卡了很久？**
在下载模型（600MB），状态栏有提示。下完这一次，以后都是秒开。想提前下好，运行 `安装依赖.bat`。

**Q：模型下载失败 / 卡住？**
已默认走国内镜像 `hf-mirror.com`。如果还是不行，挂个梯子再跑一次 `安装依赖.bat`。

**Q：识别不准？**
三个办法：① 表格里双击那一行手动改；② 去「编辑类别清单」给这个类别补几条更贴切的英文描述；③ 调高把握阈值，让拿不准的都进「其它」再手工处理。

**Q：勾了「连子文件夹一起整理」会怎样？**
子文件夹里的文件也会被抓出来，统一归到你拖入的那个文件夹下的类别文件夹里。已经分好类的类别文件夹会自动跳过，不会来回搬。原来的空子文件夹会保留，不会删。

**Q：视频怎么识别的？**
从 15%、45%、75% 三个位置各抽一帧（只读，不转码），综合三帧的结果判断。勾选「视频单独归类」可以让所有视频统一进「视频」文件夹。

---

## 九、文件结构

```
photo_sorter/
├─ 启动.bat            双击运行
├─ 安装依赖.bat        第一次用先跑这个
├─ app.py              主界面
├─ categories.json     类别清单（可直接用记事本改）
├─ settings.json       记住你上次的选项（自动生成）
├─ selftest.py         自检脚本
└─ core/
   ├─ config.py        配置与类别
   ├─ media.py         只读读取图片/RAW/视频
   ├─ classifier.py    规则引擎 + 本地语义模型
   └─ organizer.py     扫描、编号、安全移动、撤销
```

整理过的文件夹里会多一个隐藏的 `.photo_sorter_undo` 目录，存的是撤销记录。不想要可以直接删，删了就没法撤销了。
